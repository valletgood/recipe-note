---
description: 프로젝트 전체 코딩 규칙
alwaysApply: true
---

# 프로젝트 코딩 규칙

## 언어 및 스타일

- TypeScript를 기본으로 사용한다
- 함수형 프로그래밍 패턴을 선호한다
- 변수명은 camelCase, 컴포넌트는 PascalCase를 사용한다
- 공통 컴포넌트 사용을 선호한다.

## 파일 구조

- 컴포넌트: `src/components/` 하위에 배치
- 유틸리티: `src/utils/` 하위에 배치
- API 호출: `src/api/` 하위에 배치

## 금지 사항

- any 타입 사용 금지
- 인라인 스타일 사용 금지
- 하드코딩된 문자열 금지 (상수 파일 사용)
